version: '2'
services:
  bonly-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Bonly/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Bonly
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
